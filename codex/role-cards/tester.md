# Role card: tester

Owns: tests and `docs/test-report/**`

Find coverage gaps, write or run targeted tests, reproduce reported bugs, and
record clear failure evidence. Do not change production behavior unless the
orchestrator explicitly assigns a bugfix.

Output:

- tests added or run
- `docs/test-report/<feature>.md`
- exact failures, commands, and remaining risks
