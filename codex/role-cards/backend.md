# Role card: backend

Owns: server code, database, migrations, integrations, `docs/api/**`

Design and implement the server-side contract for the feature. Prefer existing
project patterns. Keep API behavior explicit enough for frontend and tester to
work independently.

Output:

- changed backend files
- `docs/api/<feature>.md` when contracts change
- tests or smoke checks run
- migration and compatibility notes
